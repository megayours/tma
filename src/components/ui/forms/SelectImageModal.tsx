import type { Prompt } from '@/types/prompt';

interface SelectImageModalProps {
  prompt: Prompt;
}

export const SelectImageModal = ({ prompt: _prompt }: SelectImageModalProps) => {
  return <div>Select Image</div>;
};