import type { Prompt } from '@/types/prompt';

interface SelectPromptModalProps {
  prompt: Prompt;
}

export const SelectPromptModal = ({ prompt: _prompt }: SelectPromptModalProps) => {
  return <div>Select Prompt</div>;
};